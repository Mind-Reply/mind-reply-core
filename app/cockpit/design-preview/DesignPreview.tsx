'use client';

import { useState } from 'react';

type Concept = 'signal' | 'focus' | 'fleet';

const concepts: { id: Concept; label: string; title: string; description: string }[] = [
  { id: 'signal', label: '01 · SIGNAL', title: 'Signal Command', description: 'Fastest view: what is live, what is broken, and what to ship next.' },
  { id: 'focus', label: '02 · FOCUS', title: 'Focus Board', description: 'Simplified action-first cockpit for phone use and daily execution.' },
  { id: 'fleet', label: '03 · FLEET', title: 'Brand Fleet', description: 'A visual map of public brands, private tools, and ideas.' },
];

const c = {
  text: '#f5f7fb', muted: '#9aa6ba', line: 'rgba(255,255,255,.10)', panel: 'rgba(16,24,39,.78)', cyan: '#64e8d1', coral: '#ff8d78', amber: '#f8c56b', blue: '#84a9ff',
};

function Pill({ children, tone = c.cyan }: { children: React.ReactNode; tone?: string }) {
  return <span style={{ color: tone, border: `1px solid ${tone}55`, background: `${tone}12`, borderRadius: 999, padding: '5px 9px', fontSize: 11, fontWeight: 700, letterSpacing: '.06em', whiteSpace: 'nowrap' }}>{children}</span>;
}

function Panel({ children, style = {} }: { children: React.ReactNode; style?: React.CSSProperties }) {
  return <div style={{ background: c.panel, border: `1px solid ${c.line}`, borderRadius: 18, padding: 18, ...style }}>{children}</div>;
}

function Signal() {
  const metrics = [['Live', '08', c.cyan, 'all clear'], ['Attention', '03', c.amber, 'needs review'], ['Blocked', '01', c.coral, 'action needed']];
  return <div style={{ display: 'grid', gridTemplateColumns: 'minmax(0,1.45fr) minmax(260px,.8fr)', gap: 16 }}><div style={{ display: 'grid', gap: 16 }}><div style={{ display: 'grid', gridTemplateColumns: 'repeat(3,minmax(0,1fr))', gap: 10 }}>{metrics.map(([label, value, color, detail]) => <Panel key={label} style={{ padding: 16 }}><div style={{ color: c.muted, fontSize: 12 }}>{label}</div><div style={{ color, fontSize: 34, fontWeight: 800, lineHeight: 1.15, marginTop: 9 }}>{value}</div><div style={{ color: c.muted, fontSize: 11, marginTop: 5 }}>{detail}</div></Panel>)}</div><Panel><div style={{ display: 'flex', justifyContent: 'space-between', gap: 12, alignItems: 'center', marginBottom: 16 }}><div><div style={{ color: c.muted, fontSize: 11, textTransform: 'uppercase', letterSpacing: '.14em' }}>Ask the estate</div><div style={{ fontSize: 19, fontWeight: 750, marginTop: 5 }}>What should I ship next?</div></div><Pill>AI READY</Pill></div><div style={{ background: '#0c1423', border: `1px solid ${c.line}`, borderRadius: 14, padding: 14, color: c.muted, fontSize: 13 }}>Clear the private access blocker, then verify the public MindReply route before adding new brand surface area.</div><div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginTop: 12 }}>{['What is live?', 'What is broken?', 'Show brands'].map(x => <Pill key={x} tone={c.blue}>{x}</Pill>)}</div></Panel><Panel><div style={{ color: c.muted, fontSize: 11, textTransform: 'uppercase', letterSpacing: '.14em' }}>Ship queue</div><div style={{ fontSize: 17, fontWeight: 750, marginTop: 5 }}>Three decisions, one clear next action</div>{[['Protect /command', 'Auth gate + noindex verification', 'NOW', c.coral], ['Verify mind-reply.com', 'Production HTTP and domain check', 'NEXT', c.amber], ['Connect AI provider', 'Add one server-side provider key', 'LATER', c.blue]].map(([title, detail, badge, color]) => <div key={title} style={{ display: 'flex', alignItems: 'center', gap: 12, padding: '12px 0', borderTop: `1px solid ${c.line}` }}><div style={{ width: 8, height: 8, borderRadius: '50%', background: color, boxShadow: `0 0 16px ${color}` }} /><div style={{ flex: 1 }}><div style={{ fontSize: 14, fontWeight: 700 }}>{title}</div><div style={{ color: c.muted, fontSize: 12, marginTop: 3 }}>{detail}</div></div><Pill tone={color}>{badge}</Pill></div>)}</Panel></div><div style={{ display: 'grid', gap: 16, alignContent: 'start' }}><Panel><div style={{ color: c.muted, fontSize: 11, textTransform: 'uppercase', letterSpacing: '.14em' }}>System pulse</div><div style={{ display: 'flex', alignItems: 'center', gap: 10, marginTop: 16 }}><div style={{ width: 12, height: 12, borderRadius: '50%', background: c.cyan, boxShadow: `0 0 20px ${c.cyan}` }} /><strong>Operational with attention</strong></div><div style={{ color: c.muted, fontSize: 12, marginTop: 12 }}>Updated just now · server-side checks only</div>{[['GitHub', 92, c.cyan], ['Vercel', 78, c.blue], ['n8n', 35, c.amber], ['AI providers', 58, c.coral]].map(([label, value, color]) => <div key={label} style={{ marginTop: 16 }}><div style={{ display: 'flex', justifyContent: 'space-between', color: c.muted, fontSize: 12, marginBottom: 5 }}><span>{label}</span><span>{value}%</span></div><div style={{ height: 5, background: '#253047', borderRadius: 99 }}><div style={{ width: `${value}%`, height: '100%', background: color, borderRadius: 99 }} /></div></div>)}</Panel><Panel style={{ background: 'linear-gradient(145deg,#15283a,#182023)', borderColor: `${c.cyan}33` }}><Pill>OWNER ONLY</Pill><div style={{ fontSize: 21, fontWeight: 800, marginTop: 14 }}>One calm surface for the whole estate.</div><div style={{ color: c.muted, fontSize: 13, lineHeight: 1.6, marginTop: 9 }}>No clutter. No fake status. Every card points to evidence or a next decision.</div></Panel></div></div>;
}

function Focus() {
  const tasks = [['Clear private access', 'Protect /command before cloud sharing', c.coral, 'NOW'], ['Verify public surface', 'Check mind-reply.com and www', c.amber, 'NEXT'], ['Review Meridian', 'Decide landing page or future concept', c.blue, 'LATER'], ['Connect workflow hub', 'Add n8n production status later', c.muted, 'LATER']];
  return <div style={{ display: 'grid', gridTemplateColumns: 'minmax(0,.9fr) minmax(0,1.4fr)', gap: 16 }}><Panel style={{ borderRadius: 22, padding: 20 }}><div style={{ color: c.muted, fontSize: 11, textTransform: 'uppercase', letterSpacing: '.14em' }}>Good morning, Angel</div><div style={{ fontSize: 29, fontWeight: 850, letterSpacing: '-.04em', marginTop: 12 }}>Focus on the next useful move.</div><div style={{ color: c.muted, fontSize: 13, lineHeight: 1.6, marginTop: 12 }}>The estate is broad. Today’s decision is narrow.</div><button style={{ width: '100%', marginTop: 24, padding: '13px 15px', border: 0, borderRadius: 13, background: c.cyan, color: '#06151a', fontWeight: 800 }}>Open next action</button><div style={{ marginTop: 24, paddingTop: 20, borderTop: `1px solid ${c.line}` }}><div style={{ color: c.muted, fontSize: 11, textTransform: 'uppercase', letterSpacing: '.14em' }}>At a glance</div><div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginTop: 12 }}>{[['08', 'live'], ['01', 'blocked'], ['14', 'brands'], ['06', 'ideas']].map(([value, label]) => <div key={label} style={{ background: '#0c1423', borderRadius: 13, padding: 12 }}><div style={{ fontSize: 22, fontWeight: 800 }}>{value}</div><div style={{ color: c.muted, fontSize: 11, marginTop: 2 }}>{label}</div></div>)}</div></div></Panel><Panel style={{ borderRadius: 22, padding: 20 }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}><div><div style={{ color: c.muted, fontSize: 11, textTransform: 'uppercase', letterSpacing: '.14em' }}>Today’s board</div><div style={{ fontSize: 19, fontWeight: 800, marginTop: 5 }}>Small set of controlled moves</div></div><Pill tone={c.amber}>3 ITEMS</Pill></div>{tasks.map(([title, detail, color, status]) => <div key={title} style={{ marginTop: 14, padding: 15, borderRadius: 15, border: `1px solid ${status === 'NOW' ? `${color}66` : c.line}`, background: status === 'NOW' ? `${color}0d` : '#0c1423', display: 'flex', gap: 13, alignItems: 'flex-start' }}><div style={{ width: 19, height: 19, border: `2px solid ${color}`, borderRadius: 6, marginTop: 1 }} /><div style={{ flex: 1 }}><div style={{ fontSize: 14, fontWeight: 750 }}>{title}</div><div style={{ color: c.muted, fontSize: 12, marginTop: 4 }}>{detail}</div></div><span style={{ color, fontSize: 11, fontWeight: 800 }}>{status}</span></div>)}</Panel></div>;
}

function Fleet() {
  const brands = [['MindReply', 'Public platform', c.cyan, 'LIVE'], ['AUREL', 'Brand concept', c.coral, 'REVIEW'], ['A11-K', 'Private engine', c.blue, 'PRIVATE'], ['Regex Forge', 'Public tool', c.cyan, 'LIVE'], ['Meridian', 'Luxury hospitality', c.amber, 'IDEA'], ['n8n Hub', 'Internal system', c.muted, 'PLANNED']];
  return <div><div style={{ display: 'grid', gridTemplateColumns: 'repeat(4,minmax(0,1fr))', gap: 10, marginBottom: 16 }}>{[['14', 'tracked surfaces'], ['06', 'public'], ['05', 'private'], ['03', 'needs source']].map(([value, label]) => <Panel key={label} style={{ padding: 15 }}><div style={{ color: c.cyan, fontSize: 27, fontWeight: 850 }}>{value}</div><div style={{ color: c.muted, fontSize: 11, marginTop: 4 }}>{label}</div></Panel>)}</div><div style={{ display: 'grid', gridTemplateColumns: 'repeat(3,minmax(0,1fr))', gap: 12 }}>{brands.map(([name, type, color, status]) => <Panel key={name} style={{ minHeight: 142, display: 'flex', flexDirection: 'column' }}><div style={{ display: 'flex', justifyContent: 'space-between', gap: 8, alignItems: 'flex-start' }}><div style={{ width: 34, height: 34, borderRadius: 11, background: `${color}22`, color, display: 'grid', placeItems: 'center', fontWeight: 850 }}>{name.slice(0, 1)}</div><Pill tone={status === 'LIVE' ? c.cyan : status === 'PRIVATE' ? c.blue : status === 'IDEA' ? c.amber : c.muted}>{status}</Pill></div><div style={{ fontSize: 16, fontWeight: 800, marginTop: 17 }}>{name}</div><div style={{ color: c.muted, fontSize: 12, marginTop: 4 }}>{type}</div><div style={{ marginTop: 'auto', paddingTop: 15, color, fontSize: 11, fontWeight: 750 }}>View details →</div></Panel>)}</div></div>;
}

export default function DesignPreview() {
  const [active, setActive] = useState<Concept>('signal');
  const current = concepts.find(x => x.id === active)!;
  return <main style={{ minHeight: '100vh', background: 'radial-gradient(circle at 80% 0%,#173348 0,transparent 30%),radial-gradient(circle at 0% 100%,#211e32 0,transparent 32%),#07101d', color: c.text, padding: '28px clamp(16px,4vw,64px) 64px', fontFamily: 'var(--font-inter),Inter,system-ui,sans-serif' }}><div style={{ maxWidth: 1320, margin: '0 auto' }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', gap: 20, flexWrap: 'wrap' }}><div><div style={{ color: c.cyan, fontSize: 11, fontWeight: 800, letterSpacing: '.18em' }}>MINDREPLY · PRIVATE PREVIEW</div><h1 style={{ fontSize: 'clamp(30px,5vw,58px)', lineHeight: 1, letterSpacing: '-.06em', margin: '14px 0 12px', maxWidth: 750 }}>Command Center, without the command-center clutter.</h1><p style={{ color: c.muted, maxWidth: 650, fontSize: 15, lineHeight: 1.65, margin: 0 }}>Three possible directions for one private cloud dashboard. Each keeps the outside experience simple and the owner controls close at hand.</p></div><Pill tone={c.coral}>DESIGN REVIEW · NOT PUBLIC</Pill></div><div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', margin: '30px 0 16px' }}>{concepts.map(x => <button key={x.id} onClick={() => setActive(x.id)} style={{ cursor: 'pointer', textAlign: 'left', color: active === x.id ? '#06151a' : c.muted, background: active === x.id ? c.cyan : 'transparent', border: `1px solid ${active === x.id ? c.cyan : c.line}`, borderRadius: 12, padding: '10px 13px', minWidth: 160 }}><div style={{ fontSize: 11, fontWeight: 800 }}>{x.label}</div><div style={{ fontSize: 13, fontWeight: 750, marginTop: 4 }}>{x.title}</div></button>)}</div><div style={{ color: c.muted, fontSize: 13, marginBottom: 17 }}>{current.description}</div><section style={{ background: 'rgba(8,15,27,.55)', border: `1px solid ${c.line}`, borderRadius: 26, padding: 'clamp(14px,3vw,26px)', boxShadow: '0 24px 90px rgba(0,0,0,.25)', overflow: 'hidden' }}>{active === 'signal' ? <Signal /> : active === 'focus' ? <Focus /> : <Fleet />}</section><div style={{ display: 'flex', justifyContent: 'space-between', gap: 16, flexWrap: 'wrap', color: c.muted, fontSize: 12, marginTop: 18 }}><span>Preview route: <code>/cockpit/design-preview</code></span><span>Next step: choose one visual direction before production wiring.</span></div></div></main>;
}
